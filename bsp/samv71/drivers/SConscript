from building import *

cwd = GetCurrentDir()
src = Glob('board.c')
src += Glob('drv_usart.c')

CPPPATH = [cwd]

group = DefineGroup('Drivers', src, depend = [''], CPPPATH = CPPPATH)

Return('group')
