source [find interface/stlink.cfg]

transport select hla_swd

source [find target/stm32f4x.cfg]

reset_config none

init

proc flash_image {bin} {
    reset halt
    flash write_image erase $bin 0x08020000

    reset
    shutdown
}
proc rst {} {
    reset
    shutdown
}
